fastapi==0.104.1
uvicorn==0.24.0
numpy==1.26.2
python-multipart==0.0.6
python-dotenv==1.0.0
opencv-python-headless==4.8.1.78
# face_recognition requires dlib compilation - install separately if needed
# pip install face_recognition
# For Windows without C++ build tools, the app runs in simulation mode
